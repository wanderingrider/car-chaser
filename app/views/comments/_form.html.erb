<% if user_signed_in? %>
  <%= form_for [@item, @comment], remote: true do |f| %> 
  <%= f.text_area :text, placeholder: "目撃情報を提供する", class: "comment-text" %>
  <%= f.submit "SEND" ,class: "comment-btn" %>
  <% end %>
  <% end %>